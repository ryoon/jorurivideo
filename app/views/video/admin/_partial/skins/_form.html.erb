<%

skins = Video::Skin.new.enabled.find(:all, :order => :sort_no)
skins = skins.collect{|c| [c.title, c.id]}

#attributes = Article::Attribute.find(:all, :conditions => {:content_id => @content.id}, :order => :sort_no)
#attributes = attributes.collect{|c| [c.title, c.id]}

%><%# f.array_select 'in_attribute_ids[0]', attributes, :include_blank => true %>
<%= f.array_select 'skin_id', skins, :include_blank => true %>
